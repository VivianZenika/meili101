{
  "name": "pokedex-seeder",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf ./build",
    "dev": "nodemon",
    "lint": "eslint . --ext .ts",
    "start": "yarn build && node build/index.js",
    "test": "yarn clean && vitest",
    "test:coverage": "yarn clean && vitest run --coverage",
    "test:watch": "vitest -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "@typescript-eslint/eslint-plugin": "^5.15.0",
    "@typescript-eslint/parser": "^5.15.0",
    "c8": "^7.11.0",
    "eslint": "^8.11.0",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2",
    "vitest": "^0.7.6"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "dotenv": "^16.3.1"
  }
}
